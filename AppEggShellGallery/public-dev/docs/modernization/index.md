# Modernization & Current Status

Where the modernization effort stands today, and where it is going. This page is the authoritative
status dashboard. Detail pages are linked from each workstream row.

## Toolchain (as of 2026-07-02, branch `modernization/rnw`)

| Component | Current | Note |
|-----------|---------|------|
| .NET SDK | **10.0.301** (`global.json`) | Build host is net10. Many project TFMs still target older frameworks; full repo-wide TFM migration is later. |
| Fable (tool) | **5.4.0** | Targets net10.0. MSBuild-direct project cracking. |
| Fable.Core | **5.0.0** | |
| Fable.React | **10.0.0-alpha.1** | No 10.x stable yet; alpha is the only Fable-5-compatible release. |
| Orleans | 3.7.2 | S10 package bump done on isolated branch; S15 spike FAILED (1/10 — custom serializer can't be deleted); S15b spike PASSED (7/7 — production codegen-host pattern proven); **S15b-production-port COMPILE GREEN (0 err 0 warn on all 5 affected projects) BUT runtime HARD BLOCKER** (Orleans 10 source generator misclassifies F# Task-CE/object-expression closures as `InterfaceImplementations`, emitting broken C#; both `GenerateCodeForDeclaringAssembly` attributes commented out -> no source-gen invokers -> runtime grain dispatch will fail). **S15c spike PASSED (session 43)**: fix = lift grain-observer object expressions to named F# classes; the FS0909 ConnectorGrain blocker was a `=>`-typo misdiagnosis. Next: **S15c-production-port** (apply fixes) → S1 (PG18 baseline). Target remains 10.2.1. |
| Frontend primitives | **`react-native-web` 0.21.2** (web) / **`react-native` 0.86.0** (native) | Off `@chaldal/reactxp` entirely; the ReactXP *name* is also gone — the seam is now `LibClient/src/Rn/` (namespace `Rn`, constructor prefix `Rn.`, `RnPrimitives.fs`). Web aliases `react-native` → `react-native-web`. **New Architecture (Fabric/TurboModules/Bridgeless) enabled** on AppTodo (verified on Android + iOS). Gallery + PerformancePlayground native config still to migrate. |
| React | **19.2.3** | Upgraded from 18.3.1. RNW 0.20 (React-19) removed `findDOMNode`/`findNodeHandle` — fixed at the one seam site. |
| Animation / gesture | RNGH **3.0.2** | Bumped from 2.21 (2.31 is JS-incompatible with RN 0.86). Reanimated 4 + Moti not yet added (New Arch unlocks them). |
| Storage | SQL Server | Postgres migration is bundled with the Orleans upgrade. **PostgreSQL-only end-state** (no permanent dual-DB); target **PG 18 + PostGIS 3.5**. Plan revised; execution begins with the spike set. |

Native and web builds are **green** on the current branch.

---

## Goals status (Goals A through K + Security)

See [Goals & Roadmap](./goals-and-roadmap.md) for full detail on each goal.

| Goal | Description | Status |
|------|-------------|--------|
| **A** | Retire the render DSL | **Done (product code).** 0 `.render` files under `Lib*`, `ThirdParty`, or `AppEggShellGallery`. 41 remain as compiler test fixtures only under `Meta/AppRenderDslCompiler/compiler.Tests/tests/`. DSL machinery deletion is the remaining closure step. |
| **B** | Fix `eggshell create-app` scaffolding | Not started. |
| **C** | Reduce component verbosity | Incremental, ongoing alongside A. `[<Component>]` + hooks path established; `~16` components still use `TapCapture` shim. |
| **D** | Standardize frontend directory structure | Incremental alongside A/B. `_autogenerated_` removal (post-DSL) is the big simplification. |
| **E** | Speed up frontend build | Partial. Render subprocess stage eliminated by Goal A. Quick wins (precompile cache, parallel type-check) not yet applied. |
| **F** | Platform baseline: .NET 10 + Fable 5 | **Done.** Migrated on branch `modernization/fable5-migration`, now in `modernization/rnw`. Backend TFM bump (net7 to net10 per project) is a separate later step. |
| **G** | PostgreSQL (PG-only) + Orleans 3.7 to 10.x upgrade | **In progress (S15c spike PASSED -- runtime blocker solved; production-port pending).** S10 package bump complete; S15 spike FAILED; **S15b spike PASSED** (7/7); **S15b-production-port COMPILE GREEN** (0 err 0 warn on Core+Host+Test+HostBuild+new C# CodegenHost) — serializer rewrite (`IGeneralizedCodec` quartet), client-builder ports (`HostBuilder.UseOrleansClient`), grain lifecycle port (`IGrainContext`, `OnActivateAsync(CancellationToken)`, `ReminderEntry.GrainId`), `UseDashboard` deferred to S11, `ApplicationPartAttribute` + `InternalsVisibleTo` wiring all in place. **BUT runtime HARD BLOCKER**: Orleans 10 source generator misclassifies F# Task-CE/object-expression closures as `InterfaceImplementations`, emitting broken C#; both `GenerateCodeForDeclaringAssembly` attributes commented out -> no source-gen invokers. **S15c spike PASSED (session 43)**: fix = lift grain-observer object expressions to named F# classes (valid C# `typeof`); FS0909 was a `=>`-typo misdiagnosis. Next: **S15c-production-port** → S1 (PG18 baseline). Target Orleans **10.2.1**, Npgsql **10.0.3**, **PG 18**. **PostgreSQL-only end-state** (no live apps -> no permanent dual-DB; SQL Server is a throwaway diff oracle). **Spike-driven** execution. Full plan in [SQL Server to Postgres](./sql-server-to-postgres.md); phase correspondence in [Phased Plan](./phased-plan.md) Phase 3 / later (P6 cut-over removed -- fresh seed only). |
| **H** | ReactXP to react-native-web seam swap | **Done + modernized.** Primitives on RN/RNW, `@chaldal/reactxp` removed, and the ReactXP *name*/seam retired (now `Rn`). Upgraded to React 19 / RN 0.86 / RNW 0.21.2 with the **New Architecture (Fabric)** enabled and verified on **Android + iOS** (AppTodo: POCO F1 + iPhone 16 simulator). Remaining: bring gallery + PerformancePlayground onto the RN 0.86 native config; adopt Reanimated 4 in components. See [ReactXP to RNW](./reactxp-to-rnw.md). |
| **I** | Frontend render hygiene (key warnings + style leaks) | In progress. Key-warning fix applied at `AppShell/Context/Context.fs`; style-leak rule codified; targeted memoization applied to several components. |
| **J** | Backend interop: standalone service + .NET client SDK | Not started. Backend is already a standalone HTTP+JSON + SignalR service with no frontend dependency. Plan: carve a `LibClient.Core` .NET NuGet SDK (transport + codecs) and publish a JS SDK. See [Backend Interoperability](./backend-interop.md). |
| **K** | Polyglot API contract: OpenAPI + generated clients | Not started. Emit an OpenAPI / JSON-Schema contract from the existing `LibCodecValidation` type walker, then generate typed clients per language (Dart, Kotlin, Swift, TS, Python) instead of hand-writing SDKs. See [Backend Interoperability](./backend-interop.md). |
| **Security** | Auth/SQL/crypto hardening | Not started. See [Security Review](./security-review.md) for the candidate findings table. |

---

## Phases status (Phases 0 through 6)

See [Phased Plan](./phased-plan.md) for full per-phase detail.

| Phase | Description | Status |
|-------|-------------|--------|
| **0** | De-risking spikes (worklets, SignalR, Orleans-on-net10) | **Done.** All three spikes passed. |
| **1** | Fable 5 + .NET 10 SDK (project TFMs stay on older targets) | **Done.** |
| **2** | SignalR modular sibling repo (`eggshell-signalr`) | **Done.** Direct `@microsoft/signalr` bindings; Fable.SignalR wrapper bypassed. |
| **3** | Backend TFM to net10, Orleans 3.7.2 frozen | **S15c spike PASSED -- runtime blocker solved; S15c-production-port pending.** Package bump complete; S15 spike FAILED; S15b spike PASSED; **S15b-production-port COMPILE GREEN** (0 err 0 warn on all 5 affected projects: Core+Host+Test+HostBuild+new C# CodegenHost) — serializer rewrite, client-builder ports, grain lifecycle port, `UseDashboard` deferred to S11, `ApplicationPartAttribute` + `InternalsVisibleTo` wiring all in place. **Runtime HARD BLOCKER**: Orleans 10 source generator misclassifies F# Task-CE/object-expression closures as `InterfaceImplementations`, emitting broken C#; both `GenerateCodeForDeclaringAssembly` attributes commented out. **S15c spike PASSED (session 43)**: fix = lift grain-observer object expressions to named F# classes (valid C# `typeof`); FS0909 was a `=>`-typo misdiagnosis. Next: **S15c-production-port** → S1 (PG18 baseline). |
| **4** | ReactXP to RN/RNW seam swap + platform upgrade | **Done (AppTodo verified on Android + iOS).** ReactXP name/seam retired to `Rn`; React 19, RN 0.86, RNW 0.21.2, RNGH 3, Reanimated 4, New Architecture (Fabric) on. AppTodo renders on the POCO F1 (`fabric:true`, debug + release) and the iPhone 16 simulator. Remaining: gallery + PerformancePlayground native, Reanimated adoption. |
| **5** | Full-stack TODO reference app + templatized scaffold | Not started. |
| **6** | Docker SQL Server + persistent dev stack | Not started. |

---

## Key cross-links

- [Architecture overview](../architecture/index.md) — the framework's backend/frontend/shared-types model.
- [Accessibility Plan](../accessibility/index.md) — the full accessibility initiative that runs alongside this modernization.
- [Runbooks: Migration Execution](../runbooks/migration-execution.md) — the executor manual for doing the mechanical migration work.
- [CLI reference](../tools/cli.md) — `eggshell` commands.
- [Goals & Roadmap](./goals-and-roadmap.md) — Goals A through K + security, short-term vs long-term framing.
- [Phased Plan](./phased-plan.md) — Phases 0 through 6, one clean table.
- [Tracked upstream Orleans issues](./tracked-orleans-issues.md) — Goal G's open/closed dotnet/orleans issues that affect our workarounds. Re-check before each Orleans work item; resolution may let us simplify the codebase.
- [RN 0.86 Upgrade: Done and Remaining](./rn86-upgrade-status.md) — detailed done-vs-remaining tracker for the ReactXP-retirement + React 19 / RN 0.86 / Fabric upgrade.
- [ReactXP to RNW](./reactxp-to-rnw.md) — the strategy and research narrative for the seam swap.
- [Render DSL Retirement](./render-dsl-retirement.md) — Goal A detail + Goals C/D + a11y/automation bar.
- [Build Performance](./build-performance.md) — Goal E: bottlenecks and ranked levers.
- [Backend Interoperability](./backend-interop.md) — Goals J and K: standalone backend, .NET NuGet client SDK (J), OpenAPI polyglot contract (K), and consumer tiers.
- [Scaffolding](./scaffolding.md) — Goal B: why `eggshell create-app` is broken and the fix plan.
- [Security Review](./security-review.md) — candidate security findings with severities.
